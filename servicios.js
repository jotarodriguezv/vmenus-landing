const barra=document.querySelector('.barra');const actualizar=()=>barra?.classList.toggle('scrolled',scrollY>24);actualizar();addEventListener('scroll',actualizar,{passive:true});const menu=document.querySelector('.menu-toggle'),enlaces=document.querySelector('.nav-enlaces');menu?.addEventListener('click',()=>{const abierto=enlaces.classList.toggle('abierto');menu.setAttribute('aria-expanded',abierto)});enlaces?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{enlaces.classList.remove('abierto');menu?.setAttribute('aria-expanded','false')}));document.querySelectorAll('.pantalla,.tv-pantalla').forEach(p=>{const frame=p.querySelector('iframe');frame?.addEventListener('load',()=>p.classList.add('cargada'))});
